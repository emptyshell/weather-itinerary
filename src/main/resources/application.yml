spring:
  datasource:
    url: jdbc:h2:mem:witinerarydb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    driver-class-name: org.h2.Driver
    username: sa
    password: password
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect

weather:
  api:
    key: @weather.api.key@
    url: @weather.api.url@

server:
  address: localhost
  port: 8080